"use client";

import { motion } from "framer-motion";

/**
 * Client wrapper for character detail page — provides entrance animation.
 */
export default function CharacterDetailClient() {
  return null; // Animation is handled via CSS; this is a placeholder for future interactivity
}
